doctype html
html(lang="en")
  head
    title koa views
    script.
      var foo = true;

      if (foo) {
         bar()
      }

      function bar() {
        console.log('everything works.');
      }

    style.
      body {
        font-family: Helvetica;
        text-align: center;
      }

      h1 {
        padding-top: 100px;
        font-weight: 200;
        color: #333;
        font-size: 4em;
      }

      h2 {
        color: #666;
        font-weight: 200;
        font-size: 2.5em;
      }

      h3 {
        font-weight: 200;
        color: #333;
        font-size: 1.5em;
      }
  body
    h1 koa-jade-workflow
    h2 基于koa + jade 的工作流 222
    h3 power by koa ♥  2014  #{title}
